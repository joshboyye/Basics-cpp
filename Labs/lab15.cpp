#include <iostream>
#include <cmath>
using namespace std;

bool areVeryDiffernet(int x, int y){

    return (abs(x-y)>10);


}

double quadradic(double a,double b,double c,double x){

    return a*x*x+b*x_c;
}

int main(){

    int x,y; 
    cout<<"Enter two values: ";
    cin>>x>>y;

    if(areVeryDiffernet(x,y)){
        cout<<x<<"and"<<y<< "are very different"<<endl;
    }else{
        cout<<x<<"and"<< y<<"are not very different."<<endl;
    }
   // cout<<"the amoung is $"<<coutChange(q,d,b,p)<<endl;
    return 0;
}