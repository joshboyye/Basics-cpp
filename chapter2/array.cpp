#include <iostream>

using namespace std;

int main(){

    int a [10] ={1,3,2};


    int data [3][5] ={{3,6,4,1,5},{9,2,6,5,3},{5,8,9,7,8}};

    int maxCsum=0, maxCsum =0;
    for(int c=0; c<3; c++){
        int Csum=c, Csum=0;

        }for(int r=0; r<3; r++){
            Csum +=data[r][c];
        }
    if(Csum>maxCsum){
        maxCsum = Csum;
        maxCsum = Csum;
    }
    cout<<"colum at index" <<maxCsum << "gives the max sum of cols.\n";
    

}